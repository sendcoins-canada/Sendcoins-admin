# =============================================================================
# SendCoins Admin Backend - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://user:password@localhost:5432/sendcoins

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
PORT=4005
NODE_ENV=development

# -----------------------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------------------
# REQUIRED in production - use a strong random string (min 32 characters)
JWT_SECRET=your-jwt-secret-here-change-in-production

# Password setup/reset token TTL (e.g., "1h", "24h", "7d")
ADMIN_PASSWORD_SETUP_TTL=24h
ADMIN_PASSWORD_RESET_TTL=1h

# -----------------------------------------------------------------------------
# Frontend URL (for email links)
# -----------------------------------------------------------------------------
ADMIN_FRONTEND_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# Email - Primary (Zepto Mail) — same as main sendcoins backend (functions/mail.js)
# -----------------------------------------------------------------------------
# Username must be "emailapikey". Password = Send Mail Token from Zepto SMTP/API → API tab.
ZEPTO_HOST=smtp.zeptomail.com
ZEPTO_PORT=587
ZEPTO_USER=emailapikey
ZEPTO_API_KEY=your-send-mail-token-from-zepto-api-tab

# Email sender address (must be a verified sender in Zepto)
MAIL_FROM=noreply@sendcoins.com

# -----------------------------------------------------------------------------
# Email - Fallback (Gmail SMTP)
# -----------------------------------------------------------------------------
# Optional - used when Zepto Mail fails
SMTP_HOST=smtp.gmail.com
SMTP_MAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# -----------------------------------------------------------------------------
# MFA Configuration
# -----------------------------------------------------------------------------
MFA_ISSUER_NAME=SendCoins Admin

# -----------------------------------------------------------------------------
# Swagger Documentation (Production Only)
# -----------------------------------------------------------------------------
# HTTP Basic Auth credentials for /api/docs in production
SWAGGER_USER=admin
SWAGGER_PASSWORD=your-secure-swagger-password

# -----------------------------------------------------------------------------
# CORS (Production)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins. Required when frontend is on different domain.
# Example: https://sendcoins-admin-frontend.vercel.app,https://admin.sendcoins.ca
# Leave empty to allow all origins (dev only).
# CORS_ORIGINS=https://your-frontend.vercel.app

# -----------------------------------------------------------------------------
# Vercel Deployment (Optional)
# -----------------------------------------------------------------------------
# Set to any value to enable Vercel-specific configurations
# VERCEL=1
