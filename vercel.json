{
  "version": 2,
  "buildCommand": "prisma generate && nest build",
  "routes": [
    { "src": "/(.*)", "dest": "dist/src/main.js", "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS", "HEAD"] }
  ]
}
